# Library Management System - Full Stack (React + Express + MySQL)

Run both servers (from root):

npm install
npm run dev

Admin credentials (app user)
Email: admin@example.com
Password: admin123

Database: it assumes you already created the 'kitabghar' database in MySQL (phpMyAdmin) and imported tables.
